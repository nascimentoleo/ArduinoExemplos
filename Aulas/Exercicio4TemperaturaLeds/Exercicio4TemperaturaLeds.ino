byte pin8 = 8;
byte pin9 = 9;
byte pin10 = 10;
byte pin11 = 11;

void setup() {
  pinMode(pin8, OUTPUT);
  pinMode(pin9, OUTPUT);
  pinMode(pin10, OUTPUT);
  pinMode(pin11, OUTPUT)

}

void loop() {
  // put your main code here, to run repeatedly:

}
